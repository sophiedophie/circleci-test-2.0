version: 2.1
orbs:
  win: circleci/windows@2.4.0
jobs:
  build:
    executor:
      name: win/default
      shell: powershell.exe
    environment:
      GO111MODULE: \"on\"
    steps:
      - checkout
      - run: echo "bash? and just ddskip"
workflows:
  workflow:
    jobs:
      - build